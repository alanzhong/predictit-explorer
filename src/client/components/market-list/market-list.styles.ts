import { style } from 'typestyle';

export const marketListClass = style({
  display: 'flex',
  flexDirection: 'column'
});

export const contentClass = style({
  padding: 5
});

export const marketCountClass = style({
  fontSize: '0.7em',
  marginBottom: 20
});
