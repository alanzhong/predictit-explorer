import { style } from 'typestyle';
import { Colors } from '../colors';

export const favoriteIconClass = style({
  cursor: 'pointer',
  fill: Colors.LIGHT_GRAY
});

export const favoriteClass = style({
  fill: Colors.RED
});
