import { classes, style } from 'typestyle';

export const axisContainerClass = style({
  fill: 'none',
  stroke: '#000000',
  strokeWidth: 1
});

export const tickClass = style({
  fill: 'none'
});

export const tickTextClass = style({
  fill: '#000',
  stroke: 'none',
  fontSize: '0.7em'
});
