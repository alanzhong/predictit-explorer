import { style } from 'typestyle';

export const filterOptionClass = style({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  padding: 2,
  fontSize: '0.8em'
});

export const checkboxClass = style({
  marginRight: 5
});
